# CeramicaDecor: статическая верстка для передачи программисту

## 1. Цель проекта

Нужно подготовить статическую верстку сайта CeramicaDecor: HTML, CSS, JS, шрифты и изображения, которые являются частью дизайна.

Эта папка не является финальной интеграцией с внутренней системой. Итоговый результат должен быть дизайн-пакетом, который можно передать программисту. Программист затем подтянет реальные данные из внутренней системы: товары, категории, цены, описания, фотографии, SEO-поля, формы и маршруты.

Главное: в верстке должен быть понятный внешний вид всех нужных типов страниц и шаблонов, но не обязательно полный реальный каталог со всеми товарами.

## 2. Что нужно передать программисту

Программист просит дизайн:

- HTML-разметку страниц и блоков;
- CSS-стили, адаптивность, сетки, цвета, типографику;
- JS для поведения интерфейса: меню, модалки, слайдеры, фильтры, пагинация, поиск как интерфейс;
- шрифты;
- картинки, которые являются элементами дизайна: логотипы, иконки, hero-фоны, декоративные изображения, демонстрационные изображения для карточек.

Не нужно передавать как обязательную часть дизайна всю контентную базу: тысячи фото товаров, все 360-фото, полный каталог изразцов, все цены и реальные карточки. Эти данные должны подключаться из внутренней системы.

## 3. Важное отличие от исходного ТЗ

Исходный файл ТЗ `ТЗ_CeramicaDecor_новый_дизайн_Laravel.md` описывает интеграцию нового дизайна в существующий Laravel-сайт без поломки админки.

Текущее уточнение меняет формат работы:

- сейчас готовится не Laravel-интеграция, а статическая верстка;
- верстка должна быть удобной для последующей привязки к внутренней системе;
- данные из внутренней системы не нужно заранее вшивать в HTML;
- хардкод товаров допустим только как демонстрационный пример шаблона;
- все динамические места должны быть понятны программисту.

## 4. Страницы, которые должны быть на сайте

Основные страницы:

- главная: `index.html`;
- о компании: `about.html`;
- контакты: `contacts.html`;
- политика конфиденциальности: `privacy.html`;
- пользовательское соглашение: `terms.html`;
- общий каталог: `catalog.html`;
- портфолио: `portfolio.html`;
- палитра глазурей: `glaze-palette.html`;
- реставрация: `restoration.html`;
- все для монтажа: `components.html`;
- сотрудничество: `cooperation.html`;
- блог: `blog.html`;
- видеоотзывы: `contacts-gallery-review.html`;
- 404-страница: `404.html`.

Внутренние страницы для сотрудников:

- авторизация пользователя: `login.html`;
- восстановление пароля: `password-recovery.html`.

Страницы `login.html` и `password-recovery.html` нужны только для пользования внутри компании, потому что сайт тесно связан с кабинетом заказов, которым пользуются сотрудники. Обычному посетителю сайта эти страницы не нужны.

Важно: в общем доступе на сайте не должно быть никаких переходов на `login.html` и `password-recovery.html`. Их нельзя добавлять в шапку, футер, модальное окно поиска, публичные CTA-блоки и публичный `sitemap.xml`.

Разделы каталога:

- изразцы: `catalog-izrazcy.html`;
- камины: `catalog-kaminy.html`;
- биокамины: `catalog-biokaminy.html`;
- электрокамины: `catalog-elektrokaminy.html`;
- мраморные порталы: `catalog-mramornye-portaly.html`;
- камины в облицовке мрамором: `catalog-kaminy-v-oblicovke-mramorom.html`;
- русские печи: `catalog-russkie-pechi.html`;
- отопительные печи: `catalog-otopitelnye-pechi.html`;
- барбекю комплексы: `catalog-barbekyu-kompleksy.html`;
- банные печи: `catalog-bannye-pechi.html`;
- серийные модели: `catalog-seriynye-modeli.html`;
- фасадная керамика: `catalog-fasadnaya-keramika.html`;
- интерьерная керамика: `catalog-interernaya-keramika.html`;
- метлахская плитка: `catalog-metlahskaya-plitka.html`;
- сувенирная продукция: `catalog-suvenirnaya-produkciya.html`.

Страницы, которые сейчас не делаем:

- отдельная страница поиска.

## 5. Каталог и карточки

Все разделы каталога должны быть частью единой структуры каталога.

Большинство разделов каталога используют общий шаблон каталожной страницы. Позже можно изменить баннер, фильтры, карточки, изображения и тексты под конкретный раздел.

Раздел `Изразцы` находится внутри общего каталога, но у него отдельный дизайн карточек. Поэтому в `catalog-izrazcy.html` нужно сохранить первую страницу товаров, фильтры, пагинацию и карточку изразца как шаблон для программиста.

Для будущей интеграции важно:

- карточки товаров в каталоге должны открываться как полноценные отдельные страницы товара/изделия, а не как модальные окна;
- ссылка карточки должна быть обычным `<a href="...">` на детальную страницу, чтобы ее видели пользователи и поисковые роботы;
- модальные окна можно использовать для форм заявки, видео, поиска и вспомогательных интерфейсов, но не как основной способ открытия карточки товара;
- изображения, цены, названия, характеристики и описания должны быть заменяемыми данными из внутренней системы;
- на главной странице в блоке карточек реализованных проектов цены нужно показывать округленными до ближайшей 1000 рублей: например `767 994 ₽` выводить как `768 000 ₽`; это правило относится только к главной странице, на каталожных и других страницах цены не округлять, если иное не задано отдельно;
- демонстрационные карточки в HTML/JS не являются полной базой товаров.

## 6. Формы

В верстке могут быть формы на главной, странице контактов, карточках и в модальных окнах.

Формы должны показывать дизайн и ожидаемые поля:

- имя;
- телефон;
- комментарий;
- товар или страница, откуда отправлена заявка;
- согласие на обработку персональных данных.

Endpoint отправки заявок в статической верстке может быть временным. Программист должен заменить его на обработчик внутренней системы.

Нельзя превращать формы в декоративные кнопки без поведения. Даже в статике должно быть понятно, где форма, какие поля нужны и какое сообщение показывается после отправки.

## 7. Навигация

В шапке сайта должны быть основные пункты:

- каталог;
- портфолио;
- палитра глазурей;
- все для монтажа;
- сотрудничество;
- о компании;
- контакты.

Пункт `Каталог` в шапке должен быть яркой заметной кнопкой. По нажатию на кнопку раскрывается широкое меню со всеми разделами каталога. Первый пункт меню — `Весь каталог`, ведущий на `catalog.html`.

Внутри раскрывающегося меню каталога должны быть блоки:

- `Изразцы`: каталог изразцов, палитра глазурей;
- `Камины`: дровяные камины, биокамины, электрокамины;
- `Печи`: русские печи, отопительные печи, банные печи и порталы;
- `Типовые печи-камины из наличия`;
- `Барбекю комплексы`;
- `Мраморные камины и порталы`;
- `Плитка ручной работы`: кухонные фартуки, барные стойки, облицовка стен;
- `Фасадная керамика`;
- `Метлахская плитка`;
- `Реставрация`;
- `Сувенирная продукция`.

Важно для удобства мегаменю: при наведении на разделы слева правая часть с подразделами не должна переключаться мгновенно. В статической верстке в `script.js` используется задержка около 300 мс перед переключением активного раздела. Это нужно, чтобы пользователь мог вести мышку по диагонали от пункта слева к карточкам справа, не задевая случайно соседние разделы. При интеграции это поведение нужно сохранить или реализовать аналогично.

`Реставрация` не должна быть отдельным пунктом верхней навигации. Ссылка на `restoration.html` должна находиться внутри раскрывающегося меню каталога.

Пункт `Сотрудничество` в шапке должен быть обычным пунктом навигации с узким раскрывающимся списком, без оформления кнопкой. Внутри списка должны быть пункты:

- `Печники`;
- `Дизайнеры`;
- `Застройщики`;
- `Салоны каминов`;
- `Салоны плитки`.

Пункт `Все для монтажа` в шапке должен быть обычным пунктом навигации с узким раскрывающимся списком. Внутри списка должны быть пункты:

- `Каминные топки`;
- `Электроочаги`.

Пункт `О компании` в шапке должен быть обычным пунктом навигации с узким раскрывающимся списком. Внутри списка должны быть пункты:

- `Наша команда`;
- `Видеоотзывы`.

В футере должен быть полный список разделов каталога, ссылка на `Все для монтажа`, ссылка на блог, отдельный столбец контактов с телефоном, почтой и линейными иконками мессенджеров, а также служебные ссылки.

В шапке рядом с телефоном должны быть компактные линейные иконки мессенджеров: Telegram, Max и WhatsApp. Цветные иконки мессенджеров используются только в разделе контактов.

Поиск сейчас реализован как модальное окно. Отдельная страница поиска не нужна.

Внутренние страницы `login.html` и `password-recovery.html` не должны быть доступны через публичную навигацию сайта. Ссылки на них могут использоваться только во внутренних сценариях кабинета заказов или в прямых служебных маршрутах, которые задаст программист при интеграции.

## 8. SEO-заготовки

Для статической верстки нужно оставить понятные места под SEO:

- `title`;
- `description`;
- `canonical`;
- Open Graph;
- H1;
- `sitemap.xml`;
- `robots.txt`.

В финальной интеграции программист должен подставить реальные URL, canonical, SEO-тексты и sitemap из внутренней системы.

Если в файлах встречается `DOMAIN` или пустой `canonical`, это временная заглушка, которую нужно заменить при интеграции.

## 9. Изображения и assets

Нужно различать дизайн-ассеты и контент.

Дизайн-ассеты:

- логотип;
- иконки;
- шрифты;
- hero-фоны;
- декоративные изображения секций;
- изображения, которые нужны для визуального примера карточек.

Контент:

- все реальные фотографии товаров;
- весь каталог изразцов;
- все 360-фото;
- технические фото конкретных изделий.

Контентные изображения могут оставаться в рабочей папке как демо-материал, но при передаче программисту стоит отдельно решить, какие из них действительно нужны. Огромные папки с товарными фото не должны считаться обязательной частью дизайна.

Файлы `.tmp` и `.raw`, если они есть среди изображений, не являются частью чистого дизайн-пакета и требуют отдельной проверки перед архивированием.

## 10. Что должно быть динамическим

Программист должен иметь возможность подтянуть из внутренней системы:

- разделы каталога;
- товары;
- изразцы;
- цены;
- фотографии;
- характеристики;
- описания;
- фильтры;
- пагинацию;
- SEO-поля;
- контакты;
- формы и обработчики заявок;
- авторизацию сотрудников;
- восстановление пароля сотрудников;
- маршруты и права доступа для кабинета заказов.

В HTML должны остаться только шаблоны и демонстрационные примеры.

## 11. Проверка верстки

Перед передачей нужно проверить:

- главная открывается;
- все ссылки в шапке и футере ведут на существующие HTML-файлы;
- каталог открывается;
- каждый раздел каталога открывается;
- в `catalog-izrazcy.html` видна первая страница карточек изразцов;
- фильтры и пагинация изразцов работают как шаблон;
- модалки открываются и закрываются;
- формы визуально работают и показывают состояние успешной отправки;
- мобильное меню открывается и закрывается;
- 404-страница открывается и оформлена как часть сайта;
- страницы `login.html` и `password-recovery.html` открываются по прямому адресу;
- на публичных страницах нет ссылок на `login.html` и `password-recovery.html`;
- страницы корректно выглядят на 1440, 1280, 768, 390 и 360 px;
- нет ссылок на несуществующие локальные `.html`-файлы.

## 12. Что не является задачей этой верстки

Не нужно:

- подключать реальную внутреннюю систему;
- переносить Laravel Blade-шаблоны;
- писать backend;
- переносить всю базу товаров в HTML;
- вручную делать каждую товарную карточку как отдельный статический файл;
- делать отдельную страницу поиска;
- выводить внутренние страницы авторизации и восстановления пароля в публичную навигацию сайта.

## 13. Ожидаемый итог

В результате должна получиться аккуратная папка со статической версткой сайта:

- HTML-страницы всех утвержденных разделов;
- общий CSS;
- общий JS;
- дизайн-ассеты;
- шаблоны карточек и каталогов;
- шаблон 404-страницы;
- шаблоны внутренних страниц авторизации и восстановления пароля без публичных ссылок на них;
- README с пояснениями для программиста;
- понятное разделение между дизайном и контентом.

Этот пакет передается программисту, чтобы он подключил данные из внутренней системы и превратил статическую верстку в рабочий сайт.

## 14. Рабочий план подготовки пакета

Этот план составлен после первичного аудита текущей папки со статической версткой. Его нужно использовать как пошаговый чек-лист до финальной передачи программисту.

### 14.1. Зафиксировать состав дизайн-пакета

Статус: выполнено.

Сделано:

- проверен фактический список `.html`-страниц;
- добавлен файл `pages-audit.md` со списком обязательных, каталожных и дополнительных страниц;
- удалены дополнительные страницы, которые не входят в чистый дизайн-пакет: `hero-review.html`, `izrazcy.html`, `ready.html`, `reviews.html`;
- проверено, что `izrazcy.html` не хранит самостоятельные данные каталога;
- рабочим шаблоном раздела изразцов оставлен `catalog-izrazcy.html`;
- данные каталога изразцов сохранены в `izrazcy-data.js`, `izrazcy-relations.js` и `izrazcy-360-data.js`;
- поиск отдельных изразцов переключен на `catalog-izrazcy.html?article=...`;
- роль старой страницы `ready.html` перенесена в `catalog-seriynye-modeli.html`: добавлены демонстрационные карточки серий Дорф и Ритм, сохранён баннер `images/ready_banner.png`.

### 14.2. Добавить недостающие шаблоны

Статус: выполнено.

Добавлены страницы:

- `404.html` — публичная страница ошибки в общем стиле сайта;
- `blog.html` — публичный шаблон блога с демонстрационными карточками статей;
- `login.html` — внутренняя страница входа сотрудников;
- `password-recovery.html` — внутренняя страница восстановления пароля сотрудников.

Проверено:

- все обязательные страницы из README теперь существуют;
- `login.html` и `password-recovery.html` не добавлены в публичные страницы, `sitemap.xml` и `search.js`;
- внутренние страницы имеют `robots: noindex, nofollow`;
- `404.html` имеет `robots: noindex, follow`.

### 14.3. Привести навигацию к требованиям README

Статус: выполнено.

Сделано:

- шапку на всех публичных страницах;
- футер на всех публичных страницах;
- яркую кнопку `Каталог` в шапке;
- широкое меню каталога, раскрывающееся по нажатию на кнопку;
- первый пункт меню каталога должен быть `Весь каталог` со ссылкой на `catalog.html`;
- структура широкого меню каталога приведена к 11 блокам: `Изразцы`, `Камины`, `Печи`, `Типовые печи-камины из наличия`, `Барбекю комплексы`, `Мраморные камины и порталы`, `Плитка ручной работы`, `Фасадная керамика`, `Метлахская плитка`, `Реставрация`, `Сувенирная продукция`;
- пункт `Реставрация` перенесен из верхней навигации внутрь меню каталога;
- название `Комплектующие` заменено на `Все для монтажа`;
- добавлена публичная страница `Блог` со ссылкой `blog.html` в футере;
- пункт `Сотрудничество` сделан узким раскрывающимся списком: `Печники`, `Дизайнеры`, `Застройщики`, `Салоны каминов`, `Салоны плитки`;
- пункт `Все для монтажа` сделан узким раскрывающимся списком: `Каминные топки`, `Электроочаги`;
- ссылка `Все для монтажа` добавлена в футер;
- пункт `О компании` сделан узким раскрывающимся списком: `Наша команда`, `Видеоотзывы`;
- публичные ссылки должны вести только на существующие страницы;
- `login.html` и `password-recovery.html` не должны появляться в шапке, футере, поиске, публичных CTA и `sitemap.xml`.

Проверено:

- широкое меню `Каталог` начинается с `Весь каталог` на всех публичных страницах;
- футер на публичных страницах содержит полный список разделов каталога, ссылку `Все для монтажа`, ссылку на блог, отдельный столбец контактов с линейными иконками мессенджеров и основные служебные ссылки;
- футерные логотипы ведут на `index.html`, чтобы статический пакет корректно работал локально;
- локальных ссылок на несуществующие `.html`-файлы не найдено;
- ссылки на `login.html` и `password-recovery.html` остаются только между внутренними страницами сотрудников.

### 14.4. Нормализовать каталог

Статус: выполнено.

Сделано:

- `catalog.html` как общий вход в каталог;
- все 15 страниц разделов каталога;
- единый шаблон для большинства разделов;
- отдельный шаблон `catalog-izrazcy.html` для изразцов;
- фильтры, пагинацию и карточку изразца как демонстрационный шаблон;
- карточки товаров должны вести на полноценные отдельные страницы, а не открываться в модальных окнах.

Проверено и исправлено:

- `catalog.html` теперь показывает весь демонстрационный список обычного каталога;
- обычные карточки каталога сейчас имеют демонстрационную ссылку `?product=...`, но при интеграции она должна быть заменена на URL полноценной страницы товара;
- `catalog-izrazcy.html` сейчас имеет демонстрационную ссылку `?article=...`, но при интеграции она должна быть заменена на URL полноценной страницы изразца;
- `catalog-izrazcy.html` сохраняет отдельную выдачу изразцов, фильтры и пагинацию как шаблон; детальная карточка изразца должна быть отдельной страницей, а не модальным окном;
- `catalog-seriynye-modeli.html` содержит демонстрационные карточки серий Дорф и Ритм;
- пустые разделы обычного каталога оставлены как осознанные шаблоны для подключения данных из внутренней системы;
- добавлен файл `catalog-audit.md` с текущей картой разделов и количеством демонстрационных карточек.

### 14.5. Подготовить динамические зоны для программиста

Статус: выполнено.

Сделано:

- разделы каталога;
- товары;
- изразцы;
- цены;
- изображения;
- характеристики;
- описания;
- фильтры;
- пагинация;
- SEO-поля;
- контакты;
- формы и обработчики заявок.

Добавлен файл `dynamic-zones-audit.md`. В нем описано, какие данные в статике являются демонстрационными и должны заменяться из внутренней системы:

- каталог и категории;
- карточки товаров/изделий;
- изразцы;
- поиск;
- формы и заявки;
- контакты и реквизиты;
- SEO и служебные файлы;
- assets.

Отдельно отмечены большие JS-файлы данных:

- `izrazcy-data.js`;
- `izrazcy-relations.js`;
- `izrazcy-360-data.js`.

Эти файлы являются демонстрационной контентной базой, а не обязательной частью дизайна.

### 14.6. Привести формы к понятному шаблону

Статус: выполнено как аудит и требования к интеграции.

Проверены формы:

- на главной;
- на странице контактов;
- в модальных окнах;
- в каталоге;
- в разделе изразцов.

Для каждой формы должны быть понятны:

- имя;
- телефон;
- комментарий;
- товар или страница, откуда отправлена заявка;
- согласие на обработку персональных данных;
- состояние успешной отправки;
- состояние ошибки или хотя бы место под него.

Добавлен файл `forms-audit.md`. В нем зафиксированы:

- карта всех найденных форм;
- какие поля есть сейчас;
- где уже есть состояние успешной отправки;
- где есть или отсутствует состояние ошибки;
- какие данные нужно передавать при интеграции;
- рекомендуемый payload для заявок.

Текущий endpoint `https://ceramicadecor.ru/feedback/ceramicadecor_pro` считать временным. При интеграции программист должен заменить его на обработчик внутренней системы.

Важное замечание: часть форм сейчас показывает только успешное состояние и не обрабатывает ошибку сети или ответ сервера. Это нужно унифицировать при интеграции.

### 14.7. Довести SEO-заготовки

Статус: выполнено как базовая правка и аудит.

Проверено и приведено к единому виду:

- `title`;
- `description`;
- `canonical`;
- Open Graph;
- H1;
- `sitemap.xml`;
- `robots.txt`.

Сделано:

- заполнены пустые `canonical`;
- заполнены пустые и отсутствующие `og:url`;
- `DOMAIN` в `sitemap.xml` заменен на `https://ceramicadecor.ru`;
- в `robots.txt` добавлена строка `Sitemap: https://ceramicadecor.ru/sitemap.xml`;
- проверено, что у публичных HTML есть `title`, `description`, `canonical`, `og:url` и один H1;
- добавлен файл `seo-audit.md` с результатами проверки и оставшимися задачами для интеграции.

`login.html` и `password-recovery.html` не добавлять в публичный `sitemap.xml`.

Важно: на части страниц пока остаются повторяющиеся SEO-описания. При интеграции программист должен заменить их на уникальные SEO-поля из внутренней системы.

### 14.8. Почистить assets

Статус: выполнено как очистка временных файлов и аудит assets.

Разделены изображения на группы:

- дизайн-ассеты: логотип, иконки, шрифты, hero-фоны, декоративные изображения;
- демо-изображения для карточек и секций;
- контентная база товаров и изразцов;
- 360-фото;
- временные и мусорные файлы.

Перед финальной передачей нужно отдельно решить судьбу тяжелых папок с товарными фото и `images/izrazcy-360`.

Сделано:

- добавлен файл `assets-audit.md`;
- удалены временные файлы `.tmp` и `.raw` из `images/izrazcy`;
- повторно проверено, что `.tmp`, `.raw`, `.bak`, `.psd`, `.ai`, `.sketch`, `.fig` больше не найдены в проекте;
- посчитаны размеры основных asset-групп;
- отдельно отмечена тяжелая папка `images/izrazcy-360`;
- отдельно отмечены крупные JPG-баннеры в корне `images`.

Файлы `.tmp` и `.raw` не должны попадать в чистый дизайн-пакет без отдельной проверки.

### 14.9. Проверить интерфейс

Статус: выполнено как smoke-test структуры, доступности страниц и локальных ссылок.

Нужно провести smoke-test:

- главная открывается;
- каталог открывается;
- все разделы каталога открываются;
- `catalog-izrazcy.html` показывает первую страницу карточек;
- фильтры и пагинация изразцов работают как шаблон;
- поиск открывается и закрывается;
- модалки открываются и закрываются;
- формы показывают состояние отправки;
- мобильное меню открывается и закрывается;
- `404.html` открывается;
- `login.html` и `password-recovery.html` открываются по прямому адресу;
- нет ссылок на несуществующие локальные `.html`-файлы.

Проверить адаптивность минимум на ширинах:

- 1440 px;
- 1280 px;
- 768 px;
- 390 px;
- 360 px.

Сделано:

- добавлен файл `interface-audit.md`;
- проверено, что главная, каталог, все разделы каталога, `catalog-izrazcy.html`, `404.html`, `login.html` и `password-recovery.html` открываются через локальный статический сервер;
- проверено, что ключевые CSS/JS/assets доступны;
- проверено, что локальных ссылок на несуществующие `.html`-файлы не найдено;
- проверены JS-файлы через `node --check`;
- структурно подтверждены поиск, модалки, формы, мобильное меню, фильтры и пагинация изразцов;
- отмечено ограничение: визуальный клик-тест во встроенном браузере не завершен из-за сбоя браузерного инструмента, поэтому перед финальной передачей нужен короткий ручной просмотр в обычном браузере на ширинах 1440, 1280, 768, 390 и 360 px.

### 14.10. Подготовить финальную передачу программисту

Финальный пакет должен содержать:

- HTML-страницы утвержденных разделов;
- общий CSS;
- общий JS;
- шрифты;
- нужные дизайн-ассеты;
- демо-изображения, необходимые для понимания шаблонов;
- README;
- `pages-audit.md`;
- список временных заглушек и мест, которые должен заменить программист.

Перед передачей нужно не включать в чистый архив лишнюю контентную базу, если она не требуется как часть дизайна.

### Важное замечание по шаблонам карточек товаров

Сейчас в верстке есть четыре готовых шаблона отдельных страниц товара:

- `product-izrazec-template.html` — шаблон для всех карточек изразцов. При интеграции программист размножит этот шаблон и подставит нужные данные из внутренней системы.
- `product-drovyanoi-kamin-template.html` — общий шаблон для карточек в разделах: Камины, Печи, Печи-камины из наличия, Мраморные камины и порталы, Плитка ручной работы, Фасадная керамика, Метлахская плитка и Сувенирная продукция.
- `product-barbekyu-kompleks-albion-template.html` — шаблон карточки товара для раздела Барбекю комплексы.
- `product-restavraciya-izrazcovye-pechi-template.html` — шаблон карточки товара для реставрационных проектов и изделий раздела Реставрация.

Для раздела `restoration.html` добавлена одна демонстрационная карточка-заглушка реставрационного проекта, чтобы раздел не был пустым и программист видел структуру будущей карточки.

Шаблоны `product-barbekyu-kompleks-albion-template.html` и `product-restavraciya-izrazcovye-pechi-template.html` оформлены по структуре и визуальному принципу карточки `product-drovyanoi-kamin-template.html`, но нужны как отдельные типы карточек для своих разделов.

### Шаблон карточек раздела "Все для монтажа"

Добавлен файл `product-components-astov-ps-7363-template.html` - отдельная HTML-страница товара "Каминная топка АСТОВ ПС 7363" из подраздела "Каминные топки" на странице `components.html`.

Эта страница является шаблоном для всех будущих карточек товаров раздела "Все для монтажа": каминных топок, электроочагов и другого монтажного оборудования. При интеграции программист должен размножать этот шаблон и подставлять из внутренней системы название, подраздел, производителя, цену, скидку, описание, характеристики, фотографии, SEO-поля и данные формы заявки.

Первая карточка в подразделе "Каминные топки" на странице `components.html` теперь ведет на `product-components-astov-ps-7363-template.html` обычной HTML-ссылкой. Для демонстрации используются изображения `images/astov_ps7363_1.png` и `images/astov_ps7363_2.png`.
