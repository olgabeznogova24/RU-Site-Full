# CeramicaDecor — рабочие правила проекта

## Что это за проект

Это **статическая вёрстка (дизайн-пакет) для передачи программисту**, а не рабочий сайт и не Laravel-интеграция. Программист потом подтянет товары, цены, фото, SEO и формы из внутренней системы.

Главное следствие: **не вшивать данные в HTML**. В вёрстке должны быть только шаблоны и демонстрационные примеры. Не создавать отдельный HTML-файл под каждый товар.

Полное ТЗ — `readme.md`. При расхождении между этим файлом и `readme.md` источником правды считается `readme.md`.

Файл `ТЗ_CeramicaDecor_новый_дизайн_Laravel.md` устарел, описывает отменённый формат работы. Не использовать.

## Что не относится к сайту

- `Booklet OV 2026/` — печатный буклет;
- `Brandbook/` — брендбук;
- `outputs/` — рабочая папка для отладочных скриншотов, исключена из git;
- `D:\Ceramica Decor\_архив_outputs` — архив старых деплоев вне проекта.

## Правила, которые легко нарушить

- **Округление цен до 1000 ₽ — только на главной.** `767 994 ₽` выводится как `768 000 ₽`. На каталожных и других страницах цены не округлять.
- **Никаких публичных ссылок на `login.html`, `password-recovery.html` и `password-reset.html`.** Это внутренние страницы сотрудников. Их нельзя добавлять в шапку, футер, модалку поиска, публичные CTA и `sitemap.xml`. У всех трёх `robots: noindex, nofollow`. Восстановление пароля двухшаговое: `password-recovery.html` — запрос ссылки, `password-reset.html?token=...` — новый пароль.
- **Карточка товара открывается отдельной страницей, а не модальным окном.** Ссылка — обычный `<a href="...">`, чтобы её видели пользователи и поисковики. Модалки допустимы для форм заявки, видео и поиска.
- **Задержка мегаменю 300 мс** — `hoverSwitchDelay` в `script.js:312`. Нужна, чтобы вести мышку по диагонали к карточкам справа, не задевая соседние разделы. Не убирать.
- **Контейнер — 1440 px** (`--container`), контент внутри — 1360 px. Ориентир — askona.ru. Не расширять без явной просьбы: 1600 px уже пробовали 2026-07-29 и откатили 2026-07-30 как слишком широкое.
- **Шапка, баннер главной, секции и футер идут по одному рельсу — контенту `.container`.** Шапка и футер растянуты на всю его ширину через `justify-content: space-between`, свободное место уходит в промежутки. Никаких отдельных переменных и подогнанных чисел: состав шапки или подписи в футере можно менять, ничего пересчитывать не нужно. **Единственное условие — не уменьшать паддинг `.header .container` относительно остальных контейнеров,** иначе ряд шапки станет шире рельса. Подробности в `pages-audit.md`, раздел «Важное».
- **Шапка переключается на бургер-меню на 1100 px, а не на 960.** С пятью пунктами навигации она перестаёт помещаться раньше. Порог продублирован в трёх `matchMedia` в `script.js` — меняете его, меняйте в обоих файлах. Медиазапросы `960px` у остальных компонентов трогать не нужно — порог шапки отдельный.
- **`Реставрация` не отдельный пункт верхней навигации** — ссылка живёт внутри меню каталога.
- **Избранное собирается сердечками на карточках.** Логика — `favorites.js` (демо на `localStorage`, ключ `cd_favorites`), список — `favorites.html`. Кнопка на карточке `<button class="fav-btn" data-fav-toggle>` стоит в правом верхнем углу изображения, состав товара продублирован в `data-fav-*`. Добавляете новый тип карточки — добавьте туда и сердечко, иначе товар нельзя будет отметить. Клик перехватывается на фазе погружения: карточка каталога по обычному клику уходит на страницу товара.

## Ссылки на товары: правило `?product=`

Отдельных файлов под каждый товар нет и делать их не нужно. Карточка ведёт на подходящий шаблон с демонстрационным параметром, который программист заменит на реальный URL:

```
product-drovyanoi-kamin-template.html?product=kamin-versal
```

Это правило действует одинаково для каталога, результатов поиска и карточек реализованных проектов на главной.

Шаблоны товаров:

| Шаблон | Для каких разделов |
|---|---|
| `product-izrazec-template.html` | все изразцы |
| `product-drovyanoi-kamin-template.html` | камины, печи, печи-камины из наличия, мраморные камины и порталы, плитка ручной работы, фасадная керамика, метлахская плитка, сувенирная продукция, проекты в работе |
| `product-barbekyu-kompleks-albion-template.html` | барбекю комплексы |
| `product-restavraciya-izrazcovye-pechi-template.html` | реставрация |
| `product-components-astov-ps-7363-template.html` | всё для монтажа (топки, электроочаги) |

## Как править файлы

**Шапка и футер продублированы в 42 HTML-файлах.** Любую правку навигации делать скриптом по всем файлам сразу, иначе страницы разъедутся. Править вручную в одном файле — почти всегда ошибка.

**Кодировка.** Файлы в UTF-8, в git хранятся с LF (см. `.gitattributes`). Имена части файлов содержат кириллицу и пробелы. При массовых заменах через PowerShell читать и писать через `[System.Text.Encoding]::GetEncoding(28591)` (latin1, 1 байт = 1 символ) — тогда кириллица и BOM не искажаются. Обычный `Get-Content`/`Set-Content` в PowerShell 5.1 портит UTF-8.

Сам файл `.ps1` при этом должен быть чисто ASCII: PowerShell 5.1 читает скрипты без BOM как ANSI и ломается на кириллице даже в комментариях. Если во вставляемом тексте есть русские буквы, положить его в отдельный UTF-8 файл и прочитать так же побайтово: `$latin1.GetString([System.IO.File]::ReadAllBytes($path))`.

**Инлайновый CSS.** В 44 страницах есть блоки `<style>` (в `catalog.html` и `restoration.html` — по 30 КБ). Общие стили живут в `style.css`.

**Осторожно с CSS, привязанным к именам файлов.** Встречаются селекторы вида `img[src$="имя.jpg"]`. При замене картинки такие правила надо искать и обновлять, иначе они молча ломают кадрирование.

## Изображения

Различать дизайн-ассеты (логотип, иконки, шрифты, hero-фоны, декоративные изображения) и контент (фото товаров, изразцы, 360-фото).

Рабочие версии картинок названы по схеме `banner-<раздел>_desktop.webp` / `_mobile.webp` (130–420 КБ) и `contacts_showroom_NN_sm.jpg`. Рядом лежат исходники по 20–47 МБ с именами вида `banner_kaminy.jpg`, `contacts_9.jpg` — **их нельзя подключать в вёрстку**.

Перед подстановкой картинки в карточку проверять пропорцию: карточки мегаменю ждут ~1.50 (например 1600×1067), широкие `_desktop`-баннеры 1920×620 для карточек не подходят.

Список неиспользуемых файлов — `images-unused-review.md` (рабочий документ, можно удалить).

## Проверка перед передачей

- нет ссылок на несуществующие локальные `.html`;
- все картинки из `src`/`srcset` существуют на диске;
- открываются главная, каталог, все 16 разделов, `catalog-izrazcy.html` с первой страницей карточек, `404.html`, `login.html`, `password-recovery.html`, `password-reset.html`;
- работают фильтры, пагинация, модалки, поиск, мобильное меню;
- формы показывают состояние успешной отправки;
- ширины 1440, 1280, 768, 390, 360 px.

Endpoint форм `https://ceramicadecor.ru/feedback/ceramicadecor_pro` (37 мест в коде) — временный, программист заменит.

## Git

- Один коммит = одно законченное осмысленное изменение. Описание по сути, не датой.
- **Не пушить на GitHub без явной просьбы.** Коммитить можно.
- Репозиторий тяжёлый (`.git` ~1,4 ГБ): в истории лежат исходники JPG по 30–47 МБ. Удаление файлов размер истории не уменьшает.

## Аудит-файлы

Самые ценные для программиста — `dynamic-zones-audit.md` (карта демо-данных) и `forms-audit.md` (поля форм и payload заявки).

| Файл | Актуален на |
|---|---|
| `pages-audit.md` | 2026-07-23, раздел «Часто задаваемые вопросы» — 2026-07-28, избранное — 2026-07-29, раздел «Важное» — 2026-07-30 |
| `catalog-audit.md` | 2026-07-22, раздел «Проекты в работе» — 2026-07-23, раздел «Изразцы» и сердечки в карточках — 2026-07-29, раздел «Карточки и прямые ссылки» — 2026-07-30 |
| `interface-audit.md` | геометрия и ширины — 2026-07-30, избранное — 2026-07-29, остальной интерактив — 2026-07-28 |
| `seo-audit.md` | 2026-07-22, исключения из индексации — 2026-07-29 |
| `dynamic-zones-audit.md` | частично 2026-07-23 (разделы про карточки главной, страниц сотрудничества, восстановление пароля и проекты в работе), разделы «Изразцы» и «Часто задаваемые вопросы» — 2026-07-28, выдача карточек изразцов и раздел «Избранное» — 2026-07-29 |
| `forms-audit.md` | 2026-05-29, разделы про внутренние страницы — 2026-07-23, стандартная модалка заявки — 2026-07-29 |
| `security-audit.md` | 2026-05-29, разделы про внутренние страницы — 2026-07-23 |
| `assets-audit.md` | 2026-05-29, баннеры `faq.html` и `catalog-proekty-v-rabote.html` — 2026-07-23 |

При правках, меняющих состав страниц или разделов каталога, обновлять `pages-audit.md` и `catalog-audit.md` — иначе программист получит документ, который выглядит достоверным, но врёт.

Визуальный клик-тест проведён 2026-07-30 на ширинах 360/390/768/1024/1440 px, после сужения контейнера до 1440 и растяжки шапки/футера (`interface-audit.md`, раздел «Клик-тест 2026-07-30»). В ходе теста починены два бага: порог 960 в `matchMedia` `script.js` (полоса 961–1100) и пересоздание модалки заявки после успешной отправки.

`noindex` осознанно стоит только на `login.html`, `password-recovery.html`, `password-reset.html`, `favorites.html` и `404.html`. Остальные публичные страницы, включая `privacy.html`, индексируются. `favorites.html` — персональный список, поэтому вне индекса и вне `sitemap.xml`, но ссылки на неё в шапке и мобильном меню публичные.
