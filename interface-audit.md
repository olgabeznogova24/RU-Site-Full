# Аудит интерфейса CeramicaDecor

Дата проверки: 2026-07-22, раздел про баннеры — 2026-07-23. Предыдущая проверка: 2026-05-29.

## Что проверено

Проведен smoke-test по пункту 14.9 из `readme.md`: доступность HTML-страниц, локальные ссылки, ключевые JS/CSS/assets, наличие интерактивных элементов и подготовка адаптивных правил.

Разделение по свежести данных:

- **перепроверено 2026-07-22**: состав страниц, локальные ссылки, существование изображений, ссылки карточек главной;
- **данные от 2026-05-29, повторно не проверялись**: HTTP-ответы через локальный сервер, `node --check` для JS, структурная проверка интерактивных зон.

## Состав страниц

Проверено 2026-07-22 по файлам на диске: 40 HTML-файлов, все страницы из `readme.md` на месте. 2026-07-23 добавлены второй шаг восстановления пароля `password-reset.html` и страница частых вопросов `faq.html`, файлов стало 42. Полный список — в `pages-audit.md`.

Разделы `catalog-pech-kaminy.html` и `catalog-bannye-portaly.html`, числившиеся в прошлой версии этого аудита, в проекте отсутствуют. Появились `catalog-metlahskaya-plitka.html`, `catalog-kaminy-v-oblicovke-mramorom.html` и `catalog-mramornye-portaly.html`.

## Локальные ссылки

Проверены `href`, которые ведут на локальные `.html`-файлы, и все `src`/`srcset` изображений.

Результат проверки 2026-07-22:

- ссылок на несуществующие локальные `.html`-файлы не найдено;
- все изображения из `src` и `srcset` существуют на диске.

Исправлено 2026-07-22: четыре карточки на главной вели на несуществующие `product-*-template.html`. Переведены на существующие шаблоны с параметром `?product=<slug>`.

## Ключевые assets

| Файл | Статус |
|---|---:|
| `style.css` | 200 |
| `script.js` | 200 |
| `search.js` | 200 |
| `izrazcy-data.js` | 200 |
| `izrazcy-relations.js` | 200 |
| `izrazcy-360-data.js` | 200 |
| `images/logo.png` | 200 |
| `images/favicon.svg` | 200 |

JS-файлы `script.js`, `search.js`, `izrazcy-data.js`, `izrazcy-relations.js`, `izrazcy-360-data.js` проходят `node --check` без синтаксических ошибок.

## Интерактивные зоны

Структурно подтверждено:

- мобильное меню подключено через `burgerBtn`, классы `open` и `active`;
- поиск создается в `search.js` как `siteSearchModal`, открывается кнопками `searchBtn` и `searchBtnMobile`, закрывается через close/backdrop/Escape;
- общая модалка заявки `requestModal` открывается и закрывается через `openRequestModal`/`closeRequestModal`;
- формы показывают состояние отправки: контактная форма временно меняет текст кнопки на `Отправлено!`, модальные и изразцовые формы выводят сообщение благодарности;
- `catalog-izrazcy.html` содержит `izrazcyGrid`, `izrazcyPagination`, `PER_PAGE = 24`, обработчики фильтров и пагинации;
- карточки изразцов, модалка изразца и заявка из карточки реализованы как шаблонные интерактивные зоны.

## Адаптивность

В CSS найдены правила для ключевых диапазонов:

- desktop: правила для 1280+ и широких экранов;
- tablet: правила для 1024/960/900/768 px;
- mobile: правила для 600/560/500/480/460/375 px.

Требуемые ширины 1440, 1280, 768, 390 и 360 px покрыты существующими брейкпоинтами. Отдельный визуальный просмотр на каждой ширине перед финальной передачей все равно желателен.

## Баннеры страниц

2026-07-23: баннеры разделов каталога приведены к высоте баннеров `portfolio.html` и `contacts.html` (`min-height: 60vh`, содержимое центрируется по вертикали), см. `catalog-audit.md`.

Тогда же описание под заголовком на всех баннерах стало белым (`color: #fff`). Раньше приглушённый серый `rgba(255,255,255,0.6…0.68)` стоял на 26 страницах, белый — только на пяти страницах сотрудничества и на главной. Кадрирование каждой картинки задаётся `object-position`: для разделов каталога — в блоке «Catalog banner image positions» в `style.css` (там значения с `!important`, они перебивают инлайновый CSS страниц), для остальных страниц — в их собственных `<style>`.

## Клик-тест на пяти ширинах: выполнен

Пункт плана 14.9 из `readme.md` закрыт 2026-07-28.

История:

- 2026-05-29: автоматический клик-тест не запустился, браузерный инструмент аварийно завершался при подключении;
- 2026-07-22: в браузере частично проверено мегаменю на ширине 1440 px;
- 2026-07-28: клик-тест проведён во встроенном браузере (Chromium) на ширинах 360, 768 и 1280 px, ранее в тот же день — выборочно на 1440 и 390 px.

Что проверено 2026-07-28:

- 16 страниц на 360 px (главная, каталог, изразцы, камины, реставрация, палитра глазурей, всё для монтажа, сотрудничество, FAQ, блог, контакты, 404, карточка товара, портфолио, вход, восстановление пароля): горизонтального переполнения нет, шапка, футер и один H1 на месте;
- 5 страниц на 768 px и 5 на 1280 px — то же самое;
- мегаменю (12 вкладок, открытие/закрытие, без выхода за экран на 1280), мобильное меню (открытие/закрытие на 360–390), модалка поиска (открытие, живые результаты, закрытие), модалка заявки на карточке товара (заполнение, чекбокс согласия, состояние успешной отправки — endpoint при проверке подменялся, реальная заявка не отправлялась);
- фильтры и пагинация изразцов: переключение страниц 1/2/63, подсветка активной, неактивная кнопка «вперёд» на последней странице, пересчёт при фильтрации, сетка 3/1 колонки на 1280/768;
- таблицы SEO-блока изразцов на 360 px прокручиваются внутри своей обёртки, страницу не распирают;
- ошибок в консоли ни на одной проверенной странице нет.

Примечание: часть страниц с первой попытки не открылась из-за лимита вкладок встроенного браузера; после очистки вкладок `blog.html` и `blog-article-template.html` проверены на 360 px успешно.

## Вывод

Smoke-test по структуре пакета и визуальный клик-тест пройдены: страницы и ключевые assets на месте, битых ссылок нет, интерактив работает, адаптивность на ширинах 360–1440 px без горизонтальных переполнений.
